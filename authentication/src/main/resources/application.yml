eureka:
  client:
    service-url:
      DEFAULT_URL: http://localhost:8761/eureka/
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ybing_auth?autoReconnect=true&useUnicode=true&createDatabaseIfNotExist=true&characterEncoding=utf8
    username: root
    password:
  jpa:
    database: mysql
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  liquibase:
    change-log: classpath:/db/changelog/master.xml