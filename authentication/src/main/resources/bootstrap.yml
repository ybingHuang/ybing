spring:
  application:
    name: authentication
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      initial-size: 10
      max-active: 20
      min-idle: 5
      max-wait: 3000
#  mvc:
#    servlet:
#      path: /auth


server:
  port: 18081
#  servlet:
#      context-path: /
logging:
  file: logs/spring.log
  file.max-size: 10MB
  file.max-history: 3