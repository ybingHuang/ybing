spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          lower-case-service-id: true
#      routes:
#        - id: AUTHENTICATION
#          uri: lb://AUTHENTICATION
#          predicates:
#            - Path: /spring/authentication
server:
  port: 18080

logging:
  file: logs/spring.log
  file.max-size: 10MB
  file.max-history: 3